nodes:
  - id: node-2
    base_url: http://192.168.1.101:8001
    # For production, use a public or tunnelled base URL (e.g. https://node-2.engyne.space).
    # Per-node secrets should NOT live in git; use NODE_SHARED_SECRET or an out-of-band config.
    enabled: false
